

// not sure if we will need to use either of these but they were in app
// so I figured I would put them in so the server would run
exports.doPut = function(req, res){
  res.end("doPut method in routes/update.js");
};

exports.doPost = function(req, res){
  res.end("doPost method in routes/update.js");
};